# GENERATED by packages/harden. Install at /etc/fail2ban/filter.d/ergo.conf
# Validate against real Ergo logs before trusting it (log strings may differ by version).
[Definition]
failregex = ^.*failed login.*?<HOST>
            ^.*rejecting connection.*?<HOST>
            ^.*too many connections.*?<HOST>
ignoreregex =
