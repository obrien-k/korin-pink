// korin.pink static portal — progressive enhancement only.
// The page is fully functional without JS; this module exists so the
// <script type="module" src="main.js"> reference in index.html resolves
// instead of 404ing. Add client-side behavior here as the portal grows.
export {};
