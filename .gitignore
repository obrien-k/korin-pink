node_modules/
.env
.env.*
!.env.example
dist/
build/
.turbo/
# Wiki build output compiled into the portal at deploy time (Cloudflare Pages / CI)
packages/web/wiki/
# gamja web client build output, compiled into the portal at deploy time
packages/web/chat/
*.log
infra/tls/
infra/secrets/
infra/ergo-data/
# Box-local Ergo config with real oper hashes, mounted over the baked template
# (ERGO_CONF) — never commit. The committed template is packages/irc/ergo.yaml.
infra/ergo.local.yaml
.DS_Store
