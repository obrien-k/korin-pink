# GENERATED by packages/harden - reference copy of the embedded fail2ban filter.
# The startup script writes this to /etc/fail2ban/filter.d/ergo.conf itself.
# Validate against real Ergo logs before trusting it (log strings vary by version).
[Definition]
failregex = ^.*failed login.*?<HOST>
            ^.*rejecting connection.*?<HOST>
            ^.*too many connections.*?<HOST>
ignoreregex =
